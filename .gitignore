node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
test-results/
.env
.env.local
# Large media files — uploaded to Cloudinary
attached_assets/*.mp4
attached_assets/*.MOV
attached_assets/*.mov
attached_assets/*.docx
client/public/*.mp4
.vercel
temp_auto_push.bat
temp_interactive_push.bat
.env*

# Large source videos — all four are served from Cloudinary (see landing.tsx /
# DemoPopup.tsx); the local copies were never shipped (.vercelignore excluded them)
# and only inflated the Railway Docker build context.
client/public/*.mp4

# One-off operational scripts (contain no secrets, but not part of the app)
_enable_demo_brand.cjs
